# recovery FSM — generated by tools/generate_fsm_specs.py
# Source of truth: build/spec/Evolution_Engine_Active_Spec_10_2_2.md section 8.4
# State vocabulary: spec/fsm_states_57.yaml -> fsms.recovery_fsm
# Do not hand-edit. Change the Active Contract, then regenerate.

fsm: recovery
source: build/spec/Evolution_Engine_Active_Spec_10_2_2.md section 8.4
state_registry: spec/fsm_states_57.yaml -> fsms.recovery_fsm
initial_state: DETECT_CRASH
states_count: 9
states:
- DETECT_CRASH
- ENTER_EMERGENCY_SAFE_MODE
- RECONCILE_DB_STATE
- RECONSTRUCT_FROM_CAS
- REPLAY_COMMITTED
- RESTORED_READY
- ROLLBACK_UNCOMMITTED
- SCAN_WAL_AND_CAS
- VERIFY_LAST_GEN_HASH
terminal_states:
- RESTORED_READY
transitions:
- from: DETECT_CRASH
  to:
  - SCAN_WAL_AND_CAS
- from: SCAN_WAL_AND_CAS
  to:
  - VERIFY_LAST_GEN_HASH
- from: VERIFY_LAST_GEN_HASH
  to:
  - ROLLBACK_UNCOMMITTED
  - ENTER_EMERGENCY_SAFE_MODE
- from: ROLLBACK_UNCOMMITTED
  to:
  - REPLAY_COMMITTED
- from: REPLAY_COMMITTED
  to:
  - RECONCILE_DB_STATE
- from: ENTER_EMERGENCY_SAFE_MODE
  to:
  - RECONSTRUCT_FROM_CAS
- from: RECONSTRUCT_FROM_CAS
  to:
  - RECONCILE_DB_STATE
- from: RECONCILE_DB_STATE
  to:
  - RESTORED_READY
  - ENTER_EMERGENCY_SAFE_MODE
