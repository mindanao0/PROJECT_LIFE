# deployment FSM — generated by tools/generate_fsm_specs.py
# Source of truth: spec/ACTIVE_CONTRACT.md section 19.2
# State vocabulary: spec/fsm_states_57.yaml -> fsms.deployment_fsm
# Do not hand-edit. Change the Active Contract, then regenerate.

fsm: deployment
source: spec/ACTIVE_CONTRACT.md section 19.2
state_registry: spec/fsm_states_57.yaml -> fsms.deployment_fsm
initial_state: EXPORT_PREPARED
states_count: 8
states:
- ARCHIVED_PRODUCTION
- CANARY_EVALUATING
- CANARY_PROVISIONED
- EXPORT_PREPARED
- PACKAGE_BUNDLED
- PROMOTED_FULL_TRAFFIC
- ROLLED_BACK
- SIGNATURE_VERIFIED
terminal_states:
- ARCHIVED_PRODUCTION
- ROLLED_BACK
transitions:
- from: EXPORT_PREPARED
  to:
  - SIGNATURE_VERIFIED
  - ROLLED_BACK
- from: SIGNATURE_VERIFIED
  to:
  - PACKAGE_BUNDLED
  - ROLLED_BACK
- from: PACKAGE_BUNDLED
  to:
  - CANARY_PROVISIONED
  - ARCHIVED_PRODUCTION
  - ROLLED_BACK
- from: CANARY_PROVISIONED
  to:
  - CANARY_EVALUATING
  - ROLLED_BACK
- from: CANARY_EVALUATING
  to:
  - PROMOTED_FULL_TRAFFIC
  - ROLLED_BACK
- from: PROMOTED_FULL_TRAFFIC
  to:
  - ARCHIVED_PRODUCTION
  - ROLLED_BACK
